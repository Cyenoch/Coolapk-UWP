<Page
    x:Class="Coolapk_UWP.Pages.FeedDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Coolapk_UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Coolapk_UWP.ViewModels"
    xmlns:controls="using:Coolapk_UWP.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModels:FeedDetailViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <DataTemplate x:Key="DefaultFeedDetailTemplate">
            <TextBlock Text="{Binding}"/>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <controls:MyRichTextBlock 
            Text="{x:Bind ViewModel.Data.Message, Mode=OneWay}"
            DefaultTemplate="{StaticResource DefaultFeedDetailTemplate}"/>
    </Grid>
</Page>
