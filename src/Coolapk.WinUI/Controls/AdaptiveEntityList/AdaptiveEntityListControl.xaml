<UserControl
    x:Class="Coolapk.WinUI.Controls.AdaptiveEntityList.AdaptiveEntityListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Coolapk.WinUI.Controls.AdaptiveEntityList"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="Coolapk.WinUI.Controls.AdaptiveEntityList.AdaptiveEntityListControl"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <DataTemplate x:Key="AdaptiveItemControlSelector" x:DataType="local:EntityDataWrapper">
            <local:AdaptiveItemControlSelector Entity="{x:Bind Unwrapped, Mode=OneWay}" AdaptiveListViewModel="{x:Bind ViewModel, Mode=OneWay}"/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <TextBlock x:Name="TextA"/>
        <TextBlock x:Name="ErrorA"/>

        <ListView x:Name="DataListControl"
            IncrementalLoadingTrigger="Edge"
            IncrementalLoadingThreshold="1"
            DataFetchSize="20"
            SelectionMode="None"
            IsItemClickEnabled="False"
            ItemsSource="{x:Bind ViewModel.Data}"
            ItemTemplate="{StaticResource AdaptiveItemControlSelector}">
        </ListView>
    </Grid>
</UserControl>